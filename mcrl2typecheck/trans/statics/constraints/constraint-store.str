module statics/constraints/constraint-store

rules

  external init-constraint-store(|)

  external merge-constraint-stores(is-var,sub,glb,lub,bot,top|)

  external tell-constraint-store(is-var,sub,glb,lub,bot,top|C)

  external get-store-constraints(|)

  external constraint-store-to-string(pp|)
